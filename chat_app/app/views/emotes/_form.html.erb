<%= form_for(emote) do |f| %>
  <% if emote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(emote.errors.count, "error") %> prohibited this emote from being saved:</h2>

      <ul>
      <% emote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
